# 64 位 Windows / Linux（x86_64）：PySide6
PySide6>=6.6.0

# 32 位 Windows（x86）：PySide6 无 32 位 wheel，改用 PyQt5
# 在 32 位 Python 中执行：pip install PyQt5 pyinstaller
# PyQt5>=5.15

# Linux 在 CI（ubuntu-22.04）中用 PySide6 + PyInstaller 打包，产物为 ELF 可执行文件

